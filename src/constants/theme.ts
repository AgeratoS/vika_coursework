import {createTheme} from "@mui/material";

const theme = createTheme({
    palette: {
        primary: {
            main: '#F87C3D'
        },
        background: {
            default: '#CCCBC7'
        },
        secondary: {
            main: '#314351'
        },
        text: {
            primary: '#314351'
        }
    }
});

export default theme;
